# Screenshots

After deploying the application, capture:

1. Main dashboard with CognoDB connected.
2. Skill-gap view for Product Engineer.
3. Graph view.

Save them here as `dashboard.png`, `skill-gap.png`, and `graph.png`, then add them to the root README.

The assignment explicitly asks for screenshots of the UI, so use screenshots of the actual running application rather than mockups.
